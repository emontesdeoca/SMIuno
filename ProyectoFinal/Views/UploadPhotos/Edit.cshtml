@model ProyectoFinal.Models.Image

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Image</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.ImagePath)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ImagePath)
            @Html.ValidationMessageFor(model => model.ImagePath)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CreadoPor)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CreadoPor)
            @Html.ValidationMessageFor(model => model.CreadoPor)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.PatientModelId, "PatientModel")
        </div>
        <div class="editor-field">
            @Html.DropDownList("PatientModelId", String.Empty)
            @Html.ValidationMessageFor(model => model.PatientModelId)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
